Data tasks are implemented as PHP handlers in `app/Services/Workflows/Tasks`.

The workflow task catalog still points to this folder structure so browser/input/wait tasks and PHP data tasks are documented together.
